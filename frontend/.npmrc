# Optimizations for faster builds
prefer-offline=true
audit=false
fund=false
ignore-scripts=true
progress=false
loglevel=error
cache-max=86400000
# Disable optional dependencies to speed up install
optional=false 